# Dependencies
node_modules/

# Database (starts fresh on server)
waitlist.db

# Dev tools & screenshots
scr/
scr-check.js
screenshot.js
mobile_screenshot.js
SCROLL_VIDEO.md

# Duplicate deploy folder (not needed, Render uses the source directly)
viralconversionsrepo/

# Large video source files (not needed for deployment)
*.zip

# Mac junk
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.pyc
.env

# VC follow-up skill: credentials + generated daily task lists
.claude/skills/vc-daily-followup/config.env
vc-tasks/

# Lead/customer data exports (PII — never commit to git)
leads_export.csv
*_selected.csv
problems/*.csv

# Persoonlijke screenshots — nooit committen
**/Scherm*.png
problems/*.png
sales dash/*.png
scr map/*.png

# Lokale video-werkmap + 4K-masters. De gedeployde kopie is
# "Viralconversions website/service-videos/" (spatie-loos, static op Cloudflare).
Viralconversions website/video's website/
*-4k.mp4

# lokale database-backups (bevatten PII, nooit committen)
backups/
